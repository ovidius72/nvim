-- local ts_utils = require 'nvim-treesitter.ts_utils';

-- require('nvim_context_vt').setup {
--   custom_text_handler = function(node)
--     if node:type() == 'arrow_funcion' then
--       return 'Arrow function'
--     end
--     return ts_utils.get_node_text(node)[1]
--   end,
-- }
